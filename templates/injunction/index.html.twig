{% extends 'base.html.twig' %}

{% block title %}Injunction List{% endblock %}

{% block pageheader %}
    <h1>Injunction List</h1>
    <p class="count">
        Displaying {{ injunctions|length }} injunctions of {{ injunctions.getTotalItemCount }} total.
    </p>
{% endblock %}

{% block body %}

    <div class='btn-toolbar pull-right'>
        <div class='btn-group'>
            {% if is_granted('ROLE_CONTENT_ADMIN') %}
                <a href="{{ path('injunction_new') }}" class="btn btn-default">
                    <span class="glyphicon glyphicon-plus"></span> New </a>
            {% endif %}

            <a href="{{ path('injunction_search') }}" class="btn btn-default">
                <span class="glyphicon glyphicon-search"></span> Search </a>
        </div>
    </div>

    {% embed 'injunction/partial/table.html.twig' %}
    {% endembed %}

    <div class="navigation">
        {{ knp_pagination_render(injunctions) }}
    </div>

{% endblock %}
