{{ form_start(form) }}
{{ form_widget(form) }}
<div class="mb-3 text-center">
    <input type="submit" value="Save" class="btn btn-primary" />
    {% if county.id is null %}
        <a href="{{ path('county_index') }}" class="btn btn-warning"> Cancel </a>
    {% else %}
        <a href="{{ path('county_show', { 'id': county.id }) }}" class="btn btn-warning"> Cancel </a>
    {% endif %}
</div>
{{ form_end(form) }}

