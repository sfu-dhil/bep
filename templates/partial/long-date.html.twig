{% if entity.startDate and entity.endDate %}
    Between {{ entity.startDate }} and {{ entity.endDate }}
{% elseif entity.startDate %}
    {{ entity.startDate }}
{% else %}
    No date given
{% endif %}
