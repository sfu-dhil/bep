{% apply spaceless %}
    {% if entity.writtenDate %}
        {{ entity.writtenDate }}
    {% elseif entity.startDate and entity.endDate %}
        {{ entity.startDate }}&ndash;{{ entity.endDate }}
    {% elseif entity.startDate %}
        {{ entity.startDate }}
    {% else %}
        No date
    {% endif %}
{% endapply %}
