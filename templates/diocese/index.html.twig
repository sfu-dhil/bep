{% extends 'base.html.twig' %}

{% block title %}Diocese List{% endblock %}

{% block pageheader %}
    <h1>Diocese List</h1>
    <p class="count">
        Displaying {{ dioceses|length }} dioceses of {{ dioceses.getTotalItemCount }} total.
    </p>
{% endblock %}

{% block body %}

    <div class='btn-toolbar pull-right'>
        <div class='btn-group'>
            {% if is_granted('ROLE_CONTENT_ADMIN') %}
                <a href="{{ path('diocese_new') }}" class="btn btn-default">
                    <span class="glyphicon glyphicon-plus"></span> New </a>
            {% endif %}

            <a href="{{ path('diocese_search') }}" class="btn btn-default">
                <span class="glyphicon glyphicon-search"></span> Search </a>
        </div>
    </div>

    {% embed 'diocese/partial/table.html.twig' %}
    {% endembed %}

    <div class="navigation">
        {{ knp_pagination_render(dioceses) }}
    </div>

{% endblock %}
