{{ form_start(form) }}
{{ form_widget(form) }}
<div class="mb-3 text-center">
    <input type="submit" value="Save" class="btn btn-primary" />
    {% if source_category.id is null %}
        <a href="{{ path('source_category_index') }}" class="btn btn-warning"> Cancel </a>
    {% else %}
        <a href="{{ path('source_category_show', { 'id': source_category.id }) }}" class="btn btn-warning">
            Cancel </a>
    {% endif %}
</div>
{{ form_end(form) }}

