{{ form_start(form) }}
{{ form_widget(form) }}
<div class="mb-3 text-center">
    <input type="submit" value="Save" class="btn btn-primary" />
    <a href="{{ path('inventory_show', {'id': entity.id}) }}" class="btn btn-warning">Cancel</a>
</div>
{{ form_end(form) }}