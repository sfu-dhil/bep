<table class='table table-bordered table-condensed table-hover table-striped'>
    <tbody>
        <tr>
            <th>Transcription</th>
            <td>
                {{ inventory.transcription|raw }}
            </td>
        </tr>
        <tr>
            <th>Modern English</th>
            <td>
                {{ inventory.modifications|raw }}
            </td>
        </tr>
        <tr>
            <th>Notes</th>
            <td>
                {{ inventory.description|raw }}
            </td>
        </tr>
        <tr>
            <th>Start Date</th>
            <td>
                {{ inventory.startDate }}
            </td>
        </tr>
        <tr>
            <th>End Date</th>
            <td>
                {{ inventory.endDate }}
            </td>
        </tr>

        <tr>
            <th>Source</th>
            <td>
                {% if inventory.source %}
                    <a href='{{ path("source_show", {"id": inventory.source.id}) }}'>{{ inventory.source }}</a>
                {% endif %}
            </td>
        </tr>
        <tr>
            <th>Parish</th>
            <td>
                {% if inventory.parish %}
                    <a href='{{ path("parish_show", {"id": inventory.parish.id}) }}'>{{ inventory.parish }}</a>
                {% endif %}
            </td>
        </tr>
        <tr>
            <th>Book</th>
            <td>
                {% if inventory.book %}
                    <a href='{{ path("book_show", {"id": inventory.book.id}) }}'>{{ inventory.book }}</a>
                {% endif %}
            </td>
        </tr>
        <tr>
            <th>Created</th>
            <td>{{ inventory.created|date }}</td>
        </tr>
        <tr>
            <th>Updated</th>
            <td>{{ inventory.updated|date }}</td>
        </tr>
    </tbody>
</table>


