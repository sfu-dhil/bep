{{ form_start(form) }}
{{ form_widget(form) }}
<div class="mb-3 text-center">
    <input type="submit" value="Save" class="btn btn-primary" />
    {% if book.id is null %}
        <a href="{{ path('book_index') }}" class="btn btn-warning"> Cancel </a>
    {% else %}
        <a href="{{ path('book_show', { 'id': book.id }) }}" class="btn btn-warning"> Cancel </a>
    {% endif %}
</div>
{{ form_end(form) }}

